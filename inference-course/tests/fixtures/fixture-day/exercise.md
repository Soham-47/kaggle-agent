# Exercise

Extend the fixture lesson so that it prints a second metric, `tps`.

Acceptance hints:

- Add a variable for tokens per second and include it in the printed line.
- Run `python lesson.py --smoke`; the output includes both metric values.
- `scripts/check_day.py` accepts the folder when all checks pass.