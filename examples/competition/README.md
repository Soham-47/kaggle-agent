# Generic local competition example

This directory is a documentation-only example of the shape created by
`kaggle-agent init`. It uses synthetic `id` and `target` fields and does not
download data or submit anything.
